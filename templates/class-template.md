---
description: One short sentence - what this class is.
---

# ClassName

One or two sentences on what the class does in the game. Say so if it comes
from a framework (e.g. HurricaneVR) instead of the game itself.

## Functions

### `FunctionName`

`FunctionName(Type parameter)`

Triggered when
:   The moment the game calls it (player action, event, every frame ...).

Hook
:   Postfix, Prefix, or "none" if we only read from it.

Usage example
:   What our mod does in response. One line.

Not used
:   Parameters or fields we saw but did not need. Delete this row if empty.

## Tested class variables

| Variable | Type | Meaning | Function |
|---|---|---|---|
| `fieldName` | `float` | What it holds. | `FunctionName` |
