# Gunman Contracts modding notes

Functions we found in *Gunman Contracts Standalone*, when the game calls them, and
which variables are worth knowing. Written by modders, for modders.

Use the search (top of the page) for a class, function or variable name. To add
something, see [Contributing](contributing.md).

{{ class_overview() }}
